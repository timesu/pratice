#include <iostream>
#include <string>

using std::string;
using std::cin; using std::cout ; using std::endl;

int main()
{
    string s, p;
    //while(getline(cin, s))
     //   if(!s.empty())
      //      cout << s << endl;
    cin >> p;
    cout << p << endl;

    return 0;
}